# Local development (SQLite — no PostgreSQL install required)
DATABASE_URL="file:./dev.db"

# Production (PostgreSQL) — change provider in prisma/schema.prisma to postgresql first
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/collegehub?schema=public"

JWT_SECRET="replace-with-a-long-random-secret"
